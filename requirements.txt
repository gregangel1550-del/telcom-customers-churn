# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt — Churn Prediction Streamlit App
#
# Compatible with Python 3.9 → 3.14 (Streamlit Community Cloud uses 3.12)
#
# KEY FIX for Python 3.14 error:
#   shap pulls in numba → numba pulls in llvmlite → llvmlite fails on Python 3.14
#   Solution: install shap without its optional dependencies using --no-deps.
#   TreeExplainer (which we use) does NOT need numba or llvmlite.
#   Only KernelExplainer needs numba — we don't use it.
#
# HOW TO INSTALL (run these two commands in order):
#   pip install -r requirements.txt
#   pip install shap --no-deps
# ─────────────────────────────────────────────────────────────────────────────

# Core app
streamlit>=1.35.0

# Plotting
plotly>=5.22.0

# Data
pandas>=2.0.0
numpy>=1.24.0

# ML — unpin versions so pip picks what works with your Python
scikit-learn>=1.3.0
xgboost>=2.0.0
imbalanced-learn>=0.11.0
joblib>=1.3.0

# shap is installed separately with --no-deps (see instructions above)
# This avoids pulling in numba/llvmlite which fail on Python 3.14